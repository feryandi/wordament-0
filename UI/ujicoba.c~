#include "Menu.h"
#include "sistem.c"


int main()
{
	printf("rakarukri");
	getch();
	int a,i;
	gotoxy(0,0);
	for(a = 1; a < 24; a++)
	{
		printf("\r");
		for(i = 1; i < 80; i++)
		{
			printf(" ");
		}
		printf("\n");
	}
	gotoxy(0,0);
	getch();
	return 0;
}
